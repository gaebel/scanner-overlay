// swift-tools-version:5.2

import PackageDescription

let package = Package(
    name: "ScannerOverlay",
    products: [
        .library(
            name: "ScannerOverlay",
            targets: ["ScannerOverlay"]),
    ],
    targets: [
        .target(
            name: "ScannerOverlay",
            dependencies: [])
    ]
)
